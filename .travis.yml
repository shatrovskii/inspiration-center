language: node_js
node_js:
  - "7"
script: "gulp --youtube ${YOUTUBE_KEY} --vimeo ${VIMEO_KEY}"
after_success: ./deploy-ghpages.sh
env:
  global:
    - TARGET_BRANCH="gh-pages"
    - MASTER_BRANCH="master"
    - SOURCE_DIR="dist"
    - PUBLISH_DIR=".publish"